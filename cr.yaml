# Chart Releaser configuration file
# Most settings use sensible defaults, only override when necessary

# Skip existing releases to avoid errors on re-runs
skip-existing: true

# Mark releases as latest
mark-as-latest: true